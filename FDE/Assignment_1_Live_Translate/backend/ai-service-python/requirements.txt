fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
aiosqlite==0.20.0

# --- pick ONE LLM provider (uncomment what you use) ---
# 0.39.0 (the original pin) breaks against modern httpx (passes a removed
# `proxies=` arg). 0.116.0 is the validated working version.
anthropic==0.116.0
# google-genai==0.8.0
# openai==1.51.0
